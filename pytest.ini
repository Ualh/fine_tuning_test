[pytest]
markers =
    network: tests that reach out to external services such as Hugging Face Hub
    integration: broader integration tests that may exercise multiple components
filterwarnings =
    ignore:.*cache-system uses symlinks.*huggingface_hub.*:UserWarning
